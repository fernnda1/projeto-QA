Before('@authenticate_user') do
    puts "Implemente o código de autenticação no arquivo features/step_definitions/autehntication_steps.rb"
end

After('@authenticate_user') do
    puts "Implemente o código de autenticação no arquivo features/step_definitions/autehntication_steps.rb"
end